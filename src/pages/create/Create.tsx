import React, { Fragment } from "react";
import { Header, Create } from "../../components";

const CreatePage = () => {
    return (
        <Fragment>
            <Header />
            <Create />
        </Fragment>
    );
};

export default CreatePage;
